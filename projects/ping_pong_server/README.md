**A non-blocking server, allowing multiple concurrent UDP messages recieval and TCP connections (demonstrated with simple strings communication).**

**Includes non-blocking server administration through STDIN**
